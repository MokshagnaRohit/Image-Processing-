The program requires Wavelet toolbox to run the in built functions used. 

The input image is leena512noisy.bmp by default. 

If the input image needs to be changed to leena512.bmp then,
the 'img' command for leena512noisy.bmp can be commented and then
the 'img' command for leena512.bmp can be uncommented.